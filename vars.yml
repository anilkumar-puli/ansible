 - name: demo on vars
   hosts: localhost
  # vars_files:
   #  - example-vars.yml
   #vars:
    # url: play.example.com
    # PROJECT: example
   #vars_prompt:
    # - name: url
     #  prompt: ENTER url
   tasks:
    - name: print url
      debug:
        msg: url= {{url}}
    - name: print url from task
      debug:
        msg: url = {{url}}
      vars:
        url: task.example.com
#........varialble precedence.............
#tasks
#vars_files
#vars