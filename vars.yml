
 - name: demo on vars
   hosts: localhost
   vars:
     url: play.example.com
     PROJECT: example
   tasks:
    - name: print url
      debug:
        msg: url= {{url}}
    - name: print url from task
      debug:
        msg: url = {{url}}
      vars:
        url: task.example.com
