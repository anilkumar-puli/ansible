 - name: Todo project
   hosts: localhost
   become: yes
   tasks:

    - name: install golang
      apt:
        name: golang
        state: present
        update_cache: yes

    - name: ansible create directory example
      file:
          path: go/src
          state: directory


    - name: download foo.conf
      get_url:
        url: http://54.89.227.197:8081/repository/login/login-main.zip
        dest: /tmp/login-main.zip
        username: admin
        password: admin

    - name: ansible create directory example
        file:
          path: go/src
          state: directory

    - unarchive:
        src: /tmp/login-main.zip
        dest: /go/src

    - name: start build
      shell:
         go build
      register: shellcmd


